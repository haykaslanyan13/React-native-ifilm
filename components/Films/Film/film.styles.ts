import {StyleSheet} from "react-native";

export const filmStyles = StyleSheet.create({
    container: {
        marginTop: 40,
        marginHorizontal: 10,
        marginBottom: 40
    },
    image: {
        width: '70%',
        height: 400
    }
})
